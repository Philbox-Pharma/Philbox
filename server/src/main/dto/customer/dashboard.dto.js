import Joi from 'joi';

/**
 * DTO for dashboard query parameters (optional filters)
 */
export const getDashboardDTO = Joi.object({
  // No parameters required - can add optional filters in future if needed
});
